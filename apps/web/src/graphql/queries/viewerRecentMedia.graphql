query ViewerLibrary($collectionInfo: MediaItemCollectionInput!) {
  viewer {
    id
    mediaItems(input: { collectionInfo: $collectionInfo }) {
      nodes {
        ...MediaItemSummary
      }
      pageInfo {
        limit
        offset
      }
      totalCount
    }
  }
}

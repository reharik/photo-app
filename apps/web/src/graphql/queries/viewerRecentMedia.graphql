query ViewerRecentMedia {
  viewer {
    id
    mediaItems(
      input: {
        collectionInfo: { pageInfo: { limit: 10, offset: 0 }, sortBy: CREATED_AT, sortDir: DESC }
      }
    ) {
      nodes {
        ...MediaItemSummary
      }
      pageInfo {
        limit
        offset
      }
    }
  }
}

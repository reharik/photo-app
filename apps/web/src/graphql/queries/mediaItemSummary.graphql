fragment MediaItemSummary on MediaItem {
  id
  kind
  status
  title
  mimeType
  originalFileName
  createdAt
  reactionCounts {
    ...ReactionCounts
  }
  viewerReactions
  ...ViewableMediaItem
}

fragment MediaItemSummary on MediaItem {
  id
  kind
  status
  title
  mimeType
  originalFileName
  createdAt
  description
  takenAt
  takenAtUtcOffsetMinutes
  reactionCounts {
    ...ReactionCounts
  }
  viewerReactions {
    ...ViewerReaction
  }
  ...ViewableMediaItem
}

query AlbumSharingExtras($albumId: ID!) {
  viewer {
    id
    album(id: $albumId) {
      id
      emailShares {
        id
        email
        displayName
        hasAccount
        userId
        delivery {
          state
          at
        }
      }
      publicLink {
        token
      }
    }
  }
}

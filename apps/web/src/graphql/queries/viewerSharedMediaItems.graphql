query ViewerSharedWithMeMediaItems($collectionInfo: SharedWithMeMediaItemCollectionInput!) {
  viewer {
    id
    sharedWithMeMediaItems(input: { collectionInfo: $collectionInfo }) {
      nodes {
        ...SharedWithMeMediaItem
      }
      pageInfo {
        limit
        offset
      }
      totalCount
    }
  }
}

fragment SharedWithMeMediaItem on SharedWithMeMediaItemType {
  id
  sharedAt
  sharedBy
  mediaItem {
    ...MediaItemSummary
  }
}

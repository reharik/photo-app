fragment ReactionCounts on ReactionCounts {
  total
  byEmoji {
    ...EmojiCount
  }
}

fragment EmojiCount on EmojiCount {
  emoji
  count
}

fragment ViewerReaction on ViewerReaction {
  emoji
  id
}

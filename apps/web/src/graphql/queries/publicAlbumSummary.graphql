fragment publicAlbumSummary on PublicAlbum {
  id
  title
  coverMedia {
    ...PublicMediaItemSummary
  }
  items(
    input: {
      collectionInfo: { pageInfo: { limit: 100, offset: 0 }, sortBy: CREATED_AT, sortDir: DESC }
    }
  ) {
    nodes {
      ...PublicAlbumItemSummary
    }
    pageInfo {
      limit
      offset
    }
  }
}

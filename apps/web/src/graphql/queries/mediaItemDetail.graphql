fragment MediaItemDetail on MediaItem {
  id
  kind
  mimeType
  title
  description
  originalFileName
  createdAt
  takenAt
  reactionCounts {
    ...ReactionCounts
  }
  viewerReactions {
    ...ViewerReaction
  }

  ...ViewableMediaItem
}

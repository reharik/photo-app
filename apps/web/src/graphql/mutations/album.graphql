mutation CreateAlbum($input: CreateAlbumInput!) {
  createAlbum(input: $input) {
    data {
      albumId
    }
    errors {
      ...errorsFragment
    }
  }
}

mutation AddMediaItemsToAlbum($input: AddMediaItemsToAlbumInput!) {
  AddMediaItemsToAlbum(input: $input) {
    data {
      albumId
      albumItemIds
    }
    errors {
      ...errorsFragment
    }
  }
}

mutation DeleteAlbumItemsFromAlbum($input: DeleteAlbumItemsFromAlbumInput!) {
  DeleteAlbumItemsFromAlbum(input: $input) {
    data {
      albumId
      albumItemIds
    }
    errors {
      ...errorsFragment
    }
  }
}

mutation SetCoverMedia($input: SetCoverMediaInput!) {
  SetCoverMedia(input: $input) {
    data {
      albumId
    }
    errors {
      ...errorsFragment
    }
  }
}

mutation MarkSurfaceSeen($targetType: EntityType!, $targetId: ID!, $kind: InAppNotificationType!) {
  markSurfaceSeen(targetType: $targetType, targetId: $targetId, kind: $kind) {
    data {
      success
    }
  }
}

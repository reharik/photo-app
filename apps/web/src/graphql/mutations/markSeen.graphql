mutation MarkSeen($targetType: EntityType!, $targetId: ID) {
  markSeen(targetId: $targetId, targetType: $targetType) {
    data {
      success
    }
    errors {
      ...errorsFragment
    }
  }
}

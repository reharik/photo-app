mutation RemoveAlbumMembers($input: RemoveAlbumMembersInput!) {
  RemoveAlbumMembers(input: $input) {
    data {
      albumId
      albumMemberIds
    }
    errors {
      ...errorsFragment
    }
  }
}

/** Grid tile hover pill — photo shows through at lower opacity. */
export const gridTilePillBackground = 'rgba(20, 15, 10, 0.55)';

/** Rail overflow popover — sits over cream chrome, needs higher contrast. */
export const railPopoverBackground = 'rgba(20, 15, 10, 0.85)';

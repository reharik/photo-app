enum ReactionTargetType {
  MEDIA_ITEM
  COMMENT
}

enum ReactionEmoji {
  HEART
  COMMENT
}

type ReactionCounts {
  total: Int! # sum across all emoji
  byEmoji: [EmojiCount!]! # detailed breakdown
}

type EmojiCount {
  emoji: ReactionEmoji!
  count: Int!
}

type ViewerReaction {
  emoji: ReactionEmoji!
  id: ID!
}

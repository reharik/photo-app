type MediaItem implements Node {
  id: ID!
  kind: MediaKind!
  status: MediaItemStatus!
  mimeType: String!
  sizeBytes: Int!
  originalFileName: String!
  width: Int
  height: Int
  durationSeconds: Int
  title: String
  description: String
  takenAt: DateTime
  tags: [String!]!
  createdAt: DateTime!
  updatedAt: DateTime!
  shares: [ShareType]
  viewerOperations: [AlbumOperation!]!
}

enum AlbumOperation {
  SHARE
  EDIT_DETAILS
  ADD_ITEMS
  REMOVE_ITEMS
  EDIT_COVER
  DELETE_ALBUM
  DOWNLOAD
}

enum MediaKind {
  PHOTO
  VIDEO
}

enum MediaItemStatus {
  PENDING
  UPLOADED
  PROCESSING
  READY
  FAILED
  DELETE_PENDING
  DELETE_FAILED
}

extend type Query {
  publicAccess: PublicAccess
}

type PublicAccess {
  id: ID!
  albumId: ID!
  grantedBy: ID!
  expiresAt: DateTime
  revokedAt: DateTime
  album: PublicAlbum!
  mediaItem(id: ID!): PublicMediaItem!
}

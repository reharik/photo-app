import { enumeration } from '@reharik/smart-enum';
const input = ['shareInvite', 'albumShared', 'mediaAdded', 'comment', 'commentReply'];
export const NotificationKindEnum = enumeration('NotificationKind', {
    input,
});
//# sourceMappingURL=notificationKind.js.map
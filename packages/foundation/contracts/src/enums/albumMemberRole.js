import { enumeration } from '@reharik/smart-enum';
const input = ['owner', 'viewer', 'contributor', 'admin'];
export const AlbumMemberRoleEnum = enumeration('AlbumMemberRole', {
    input,
});
//# sourceMappingURL=albumMemberRole.js.map
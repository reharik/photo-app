import { enumeration } from '@reharik/smart-enum';
const input = ['album', 'mediaItem'];
export const ResourceTypeEnum = enumeration('ResourceType', {
    input,
});
//# sourceMappingURL=resourceType.js.map
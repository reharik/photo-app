node_modules
.env
.env.local
.env.production
.history
.vscode
dist
.ioc-generation-state.json

.nx/cache
.nx/workspace-data
.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md
vite.config.*.timestamp*
vitest.config.*.timestamp*

**/generated/
**/di/**
apps/api/src/di/

# NOTE: contracts smart-enums (src/enums/graphqlSmartEnums.ts) is deliberately COMMITTED,
# not gitignored. Although it's a codegen output, it lives in a foundation package that
# every other package consumes AS SOURCE (via tsconfig paths / jest moduleNameMapper), so
# it must be present in a fresh checkout before any typecheck/test runs. It sits alongside
# the hand-authored enums in the same directory. Regenerate + commit it when the schema changes.

# Transient artifacts (test output, captured stdout, nx graph dumps)
/test-results
/graph.json
/stdout.json


# Private keys and certificates
*.pem
*.key
*.p12
*.pfx

# Deployment artifacts
*.tar.gz
backups/

# Per-clone dev-stack override (secondary working copy only; see Makefile).
# Sets ENV_NAME and exports DEV_DB_PORT / DEV_API_PORT / DEV_LOCALSTACK_PORT.
# The port-remap compose overlay it used to point at is gone — the dev stack is
# one flat file and nothing is layered over it.
Makefile.local

# Docker volumes (if running locally)
pgdata/
caddy_data/
caddy_config/

# Optional loose schema file at repo root (API schema is tracked at apps/api/.../schema.graphql)
/schema.graphql
.obsidian/
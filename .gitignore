node_modules
.env
.env.local
.env.production
.history
.vscode
dist


.nx/cache
.nx/workspace-data
.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md
vite.config.*.timestamp*
vitest.config.*.timestamp*

# API GraphQL: *.generated.* (e.g. types.generated.ts) is local/build output, not committed
*.generated.*

# IoC (regenerated by api / worker / media-core builds; do not commit)
apps/api/src/di/generated/**
apps/media-worker/src/di/generated/**
packages/context/media-core/src/di/generated/**

# API GraphQL schema-gen outputs (downstream of hand-written schema; do not commit)
apps/api/src/graphql/generated/schema.graphql
apps/api/src/graphql/generated/types.generated.ts

apps/web/src/graphql/generated/*


# Private keys and certificates
*.pem
*.key
*.p12
*.pfx

# Deployment artifacts
*.tar.gz
backups/

# Per-clone dev-stack overrides (secondary working copy only; see Makefile)
Makefile.local
docker-compose/docker-compose.local.yml

# Docker volumes (if running locally)
pgdata/
caddy_data/
caddy_config/

# Optional loose schema file at repo root (API schema is tracked at apps/api/.../schema.graphql)
/schema.graphql
